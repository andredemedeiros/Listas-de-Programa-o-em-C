#include <stdio.h>
#include <math.h>
unsigned char calculaParidade(unsigned char b);

int main(){
    int byte,bp;
    scanf("%i%*c",&byte);
    bp=calculaParidade(byte);
    printf("Byte de paridade: %i",bp)
}
unsigned char calculaParidade(unsigned char b){

    for(int i=1;i<=8;i++){

    }
}